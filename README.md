# Relay Board frontend

This repository contains the public static frontend for Relay Board. The complete backend and deployment configuration remain in the private `xkai66/relay-board` repository.

GitHub Pages publishes this directory over HTTPS. Set the repository variable `RELAY_API_BASE` to the HTTPS backend origin before using authenticated room features.
